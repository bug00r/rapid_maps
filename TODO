NEW:
    - Create Map Structure
        - map names are unique
    - Create Map Creation Processes
        - new Map
        - save Map
            - in first Version we create a zip file with only an xml map descriptor
            - in second version we will save all images into the zip file too
                   !!!! wx.Python has No FileOutputStream !!!!
                   save image: img.SaveFile('saved.png', wx.BITMAP_TYPE_PNG)
        - edit Map
        - creation of UI Elements to handle this Process


IN PROCESS:
    - refactoring, reduce redundancies

DONE:
1. adding statemachine
2. improve selection mechanism(statemachine)
3. improve selection based(statemachine)
    - movement
    - rotation
4. improve map display
    - using tiles with canvas ans two separate scrollbars
        Zooming:
            - zoom in(making small image larger):
                - subimage from scaled viewport width and height, based on center
                - normalize this subimage to old viewport
                - adjust scrollbar
